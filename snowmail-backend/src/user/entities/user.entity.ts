export class User {
     constructor(
          public id: number, 
          public firstname: string, 
          public email: string, 
          public school: string, 
          public major: string, 
          public gpa: number, 
          public username: string,
     ) {};
}
